---
# tasks file for roles/hexo

- block:

  - name: install npm
    package:
      name: npm
      state: present
  - name: install hexo-cli
    npm:
      name: hexo-cli
      global: yes

  tags:
    - hexo
