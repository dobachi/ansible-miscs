- hosts: "{{ server | default('wsl') }}"
  roles:
    - register_home
    - commons
    - ipafont
    - git
      #    - zed
    - tmux
    - vim
    - vim_config
    - bash
    - pip3
    - pyenv
    - intellij
    - pycharm
    - goland

# vim: et ts=2 sw=2:
